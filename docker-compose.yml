services:
  product-db:
    image: mongo:7.0.4
    container_name: product-db
    ports:
      - "${MONGO_PORT}:27017"
    environment:
      - MONGO_INITDB_ROOT_USERNAME=${MONGO_USERNAME}
      - MONGO_INITDB_ROOT_PASSWORD=${MONGO_PASSWORD}
    volumes:
      - mongo_data:/data/db
  inventory-db:
    image: mysql:8
    container_name: inventory-db
    ports:
      - "${MYSQL_PORT}:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - MYSQL_DATABASE=${MYSQL_DATABASE}
    volumes:
      - inventory_data:/var/lib/mysql
  order-db:
    image: postgres:16-alpine
    container_name: order-db
    ports:
      - "${POSTGRES_PORT}:5432"
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    volumes:
      - order_data:/var/lib/postgresql/data
volumes:
  mongo_data:
  inventory_data:
  order_data:

#  ms-products:
#    build: .              # Usa el Dockerfile de la carpeta actual
#    container_name: ms-products
#    ports:
#      - "${SERVER_PORT}:8080"
#    environment:
#      # Sobrescribimos la config de Spring para usar Docker
#      # usamos 'mongodb' en lugar de 'localhost' en nuestros env porque el host es el nombre del container
#      - MONGO_HOST=${MONGO_HOST}
#      - MONGO_PORT=${MONGO_PORT}
#      - MONGO_DATABASE=${MONGO_DATABASE}
#      - MONGO_USERNAME=${MONGO_USERNAME}
#      - MONGO_PASSWORD=${MONGO_PASSWORD}
#      - MONGO_AUTH_DB=${MONGO_AUTH_DB}
#      - SERVER_PORT=${SERVER_PORT}
#    depends_on:
#      - mongodb           # Espera a que la BD arranque primero
