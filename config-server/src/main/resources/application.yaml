spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_SERVER_URI}
          clone-on-start: true
          default-label: master
server:
  port: ${CONFIG_SERVER_PORT:8888}